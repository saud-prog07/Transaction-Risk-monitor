apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: risk-monitoring
type: Opaque
stringData:
  # Database Credentials
  DB_USERNAME: "postgres"
  DB_PASSWORD: "postgres"
  
  # IBM MQ Credentials
  IBM_MQ_PASSWORD: "passw0rd"
  
  # JWT Secret (change this in production!)
  JWT_SECRET: "change-this-to-a-secure-secret-in-production-key-12345"
